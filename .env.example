# ── Docker ────────────────────────────────────────────────────────────────────

# Image name and tag used when building / pushing.
IMAGE_NAME=gravity
IMAGE_TAG=latest

# Host port the production nginx container binds to.
APP_PORT=8080

# Host port the Vite dev server binds to.
DEV_PORT=5173

# Node.js image tag used by the dev service.
NODE_VERSION=20-alpine

# Container restart policy (no | always | on-failure | unless-stopped).
RESTART_POLICY=unless-stopped

# Healthcheck timing.
HEALTHCHECK_INTERVAL=30s
HEALTHCHECK_TIMEOUT=5s
HEALTHCHECK_RETRIES=3
HEALTHCHECK_START_PERIOD=10s

# ── ElevenLabs TTS ────────────────────────────────────────────────────────────
# Used only by scripts/gen-audio.mjs to regenerate narration MP3s.
# The generated MP3s are already committed to public/audio/, so these vars are
# NOT required to build or run the app.

ELEVENLABS_API_KEY=your_api_key_here
ELEVEN_VOICE_ID=your_voice_id_here
ELEVEN_MODEL=eleven_multilingual_v2
